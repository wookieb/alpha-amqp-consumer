machine:
  node:
    version: 4.3.0
  environment:
    MOCHA_FILE: $CIRCLE_TEST_REPORTS/alpha-amqp-consumer.xml

test:
  override:
    - "npm install mocha-junit-reporter"
    - "npm run test -- --reporter mocha-junit-reporter"